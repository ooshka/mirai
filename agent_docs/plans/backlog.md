# Backlog

## Next 3 Candidate Slices

1. Filesystem Safety + Read-Only MCP File Tools (selected)
- Value: creates a safe, test-backed base for any runtime agent note access.
- Size: ~1 day.

2. Patch Proposal Validation + Apply Endpoint
- Value: enables reversible edits through constrained diffs instead of raw writes.
- Size: ~1-2 days.

3. Git Commit Wrapper + Mutation Audit Metadata
- Value: makes all note mutations reversible and auditable by default.
- Size: ~0.5-1 day.
