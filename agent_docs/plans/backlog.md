# Backlog

## Next 3 Candidate Slices

1. Route Wiring Module Extraction (selected)
- Value: reduces `app.rb` churn and keeps route-level changes reviewable as MCP endpoints expand.
- Active case: `agent_docs/cases/CASE_route_wiring_module_extraction.md`
- Size: ~0.5-1 day.

2. Runtime-Agent Action Policy Layer
- Value: introduces an explicit policy seam for runtime-agent tool permissions before adding higher-autonomy capabilities.
- Size: ~0.5-1 day.

3. Semantic Retrieval Runtime Integration
- Value: upgrades retrieval quality by wiring provider-backed retrieval behind existing `/mcp/index/query` contract.
- Size: ~1-2 days.
