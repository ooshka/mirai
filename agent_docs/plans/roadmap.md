# Roadmap (Lightweight)

1. Establish read path safety + read-only MCP tooling.
2. Add patch proposal/validation/apply flow.
3. Require git commits for every accepted mutation.
4. Add indexing/retrieval path once mutation safety is stable.
